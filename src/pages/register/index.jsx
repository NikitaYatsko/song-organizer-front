import { RegisterForm } from "@/features/register/ui/RegisterForm.jsx";

export function RegisterPage() {
    return (
        <main className="min-h-screen flex items-center justify-center px-4">
            <RegisterForm />
        </main>
    );
}